--- RCPowerLoot/comms.lua
-- Addon communication layer.
-- Handles broadcasting and receiving RPL data (BIS lists, version checks)
-- between council members using the RCLootCouncil AceComm channel.

local ADDON_NAME, ns = ...
local RPL = ns.RPL

-- Placeholder — comms logic goes here when needed.
-- (RC already handles loot history via its own comms; we extend only for
--  RPL-specific data like BIS broadcasts.)
